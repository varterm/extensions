---
description: Show what Varterm is set to and whether it can play audio
---

Useful when nothing is coming out of the speakers: it reports whether an audio
player was found at all.

Run this and show the output exactly as it comes back, with nothing added:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/varterm.mjs" status
```
